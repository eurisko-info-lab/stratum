# F12 — semantic filesystem projections and materialization-profile law

```text
semantic-fs: store language values as authority, project files, and bind projection semantics explicitly
```

## Feature

Structured content is now the repository authority for declared languages, and
surface files are generated projections under explicit materialization profiles.

$$
F_{12} = F_{11} + \text{semantic filesystem projections and materialization-profile law}
$$

## What changed

1. Tree entries persist structured-content identity and explicit materializer identity.
2. Canon-backed and grammar-backed generated checkouts are first-class.
3. Verification became constructive for patches:

$$
\operatorname{ApplyTreePatch}(T_{n-1},\delta_n)=T_n
$$

4. Verification now enforces materializer/reader/blob/grammar invariants.
5. Blocks now bind a materialization profile artifact; profile constraints are
   enforced during verification.
6. Recording supports profile selection and profile-only migration blocks when
   tree content is unchanged.

## Interpretation

The governing acceptance law remains semantic fixpoint, not byte identity:

$$
\operatorname{Parse}(\operatorname{Print}(\operatorname{Parse}(x)))=\operatorname{Parse}(x)
$$

So F12 advances Stratum as a normalizing semantic repository with explicit
projection policy.

## Result

$$
F_{12} = F_{11} + \text{semantic filesystem projections and materialization-profile law}
$$
