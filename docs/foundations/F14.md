# F14 — investigation-state agents and evidence loops

```text
agents: model investigations as stateful loops over compiled context and evidence
```

## Feature

F14 introduces an agent constitution above schema: investigation states,
compiled context, expandable semantic views, model actions, and
evidence-driven iteration.

$$
F_{14} = F_{13} + \text{investigation-state agents and evidence loops}
$$

## What changed

1. Investigation is represented as explicit, replayable state.
2. Context is compiled into stable machine-consumable form.
3. Semantic views are expandable without changing lower-layer laws.
4. Model actions are explicit and auditable.
5. Iteration is evidence-driven: new evidence advances or revises state.

## Interpretation

This keeps the agent system above schema and keeps semantic-fs focused on
repository authority and materialization law.

## Result

$$
F_{14} = F_{13} + \text{investigation-state agents and evidence loops}
$$
