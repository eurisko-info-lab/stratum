; delta-grammar for F9 -> F10: Grammar gains the retention vocabulary.
(change
  (kind delta-grammar)
  (name "F9->F10 delta-grammar")
  (replace
    (ReplaceChange
      retention-syntax
      (absent)
      (present [retain archive checkpoint collect preserve attest reconstruct]))))
