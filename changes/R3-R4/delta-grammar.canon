; R4 adds no Strata surface. The R3 grammar is inherited exactly: a causal
; block is data over sets, and both were already here.
(change
  (kind delta-grammar)
  (name "R3->R4 delta-grammar")
  (replace
    (ReplaceChange
      second-floor-grammar
      (addressing strata)
      (converging strata))))
